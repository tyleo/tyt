[package]
name = "pathspec"
version = "0.2.0"
edition = "2024"
license-file = "LICENSE"
repository = "https://github.com/tyleo/tyt"
description = "Gitignore-style glob pattern matching over logical paths."

[dependencies]
globset = "0.4"
