# voxsmith

Utilities for working with voxels, built on the `voxcore` types. Includes interchange between `voxj`, `vmax`, MagicaVoxel (`.vox`), Goxel (`.gox`), and the three Qubicle formats (`.qb`, `.qbt`, `.qbcl`).
