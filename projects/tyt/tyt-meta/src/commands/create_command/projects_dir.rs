/// Top-level workspace directory grouping every project crate. New crates are
/// scaffolded under `{PROJECTS_DIR}/<dir>`, where `<dir>` is the `--dir` group.
pub(crate) const PROJECTS_DIR: &str = "projects";
