[package]
name = "tyt-vmax"
version = "0.1.6"
edition = "2024"
license-file = "LICENSE"
repository = "https://github.com/tyleo/tyt"
description = "Commands for working with Voxel Max."
autobins = false

[[bin]]
name = "vmax"
path = "src/main.rs"
required-features = ["bin"]

[dependencies]
branded-id = "0.1.7"
clap = { version = "4.5.58", features = ["derive"] }
clap_complete = { version = "4.5", optional = true }
treegrid = { version = "0.1.0", default-features = false, features = [
    "render_hierarchy",
] }
treeselect = "0.1.0"
ty-math = { version = "0.2.0", optional = true }
tyt-common = { version = "0.1.0" }
tyt-injection = { version = "0.1.0", optional = true }
vmax-codec = { version = "0.1.0", optional = true }
voxj = { version = "0.1.3", features = ["objects"], optional = true }
voxj-codec = { version = "0.2.0", optional = true }
voxj-voxcore = { version = "0.2.0", optional = true }
voxsmith = { version = "0.2.0", optional = true }

[features]
default = ["impl"]
impl = [
    "dep:ty-math",
    "dep:tyt-injection",
    "dep:vmax-codec",
    "dep:voxj",
    "dep:voxj-codec",
    "dep:voxj-voxcore",
    "dep:voxsmith",
]
bin = ["impl", "dep:clap_complete"]
