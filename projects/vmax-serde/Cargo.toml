[package]
name = "vmax-serde"
version = "0.1.2"
edition = "2024"
license-file = "LICENSE"
repository = "https://github.com/tyleo/tyt"
description = "Serde-compatible parity types for vmax."

[dependencies]
serde = { version = "1", features = ["derive"] }
vmax = { version = "0.1.0" }
