/// The color used for every entry of a placeholder color table when an object's
/// colors are missing, so the color indices are still preserved.
pub(crate) const PLACEHOLDER_COLOR: [u8; 4] = [255, 255, 255, 255];
