/// Brand marker for a single voxel within a [`VoxObject`](crate::VoxObject)'s
/// voxel pool.
///
/// Used only as a type parameter (e.g. `IdStruct<BVoxVoxel>`); it is never
/// instantiated.
pub struct BVoxVoxel;
