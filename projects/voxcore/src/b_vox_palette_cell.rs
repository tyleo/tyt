/// Brand marker for a cell within a [`VoxPalette`](crate::VoxPalette): one
/// [`VoxValue`](crate::VoxValue) per attribute. An object's voxels sample
/// palette cells.
///
/// Used only as a type parameter (e.g. `IdStruct<BVoxPaletteCell>`); it is
/// never instantiated.
pub struct BVoxPaletteCell;
