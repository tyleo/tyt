/// Brand marker for a hierarchy node in a [`VoxState`](crate::VoxState).
///
/// Used only as a type parameter (e.g. `IdStruct<BVoxHierarchyNode>`); it is
/// never instantiated.
pub struct BVoxHierarchyNode;
