/// Brand marker for an attribute within a [`VoxPalette`](crate::VoxPalette).
///
/// Used only as a type parameter (e.g. `IdStruct<BVoxAttribute>`); it is never
/// instantiated.
pub struct BVoxAttribute;
